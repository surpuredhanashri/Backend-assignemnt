function myFunction (id:number, name:string, stream:string, marks: number)
{

     console.log (`Given data is id: ${id} name:${name} stream:${stream} marks:${marks}`)

}

myFunction (674, 'Rahul', 'CS',94);